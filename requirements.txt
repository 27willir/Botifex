Flask==3.1.2
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Session==0.8.0
Werkzeug==3.1.3
python-dotenv==1.1.1
requests==2.32.5
beautifulsoup4==4.14.2
lxml==6.0.2
selenium==4.35.0
webdriver-manager==4.0.2
loguru==0.7.3
geopy==2.4.1

# Notification dependencies
twilio==9.0.4

# Payment processing
stripe==11.1.1

# Additional dependencies for scalability (1000+ users)
# No additional packages needed - all features use Python stdlib and existing dependencies!

# For automated backup scheduling (optional)
schedule==1.2.0

# Real-time WebSocket notifications
Flask-SocketIO==5.3.6
python-socketio==5.11.1
redis==5.1.0
PyJWT==2.8.0

# API Documentation
flask-swagger-ui==4.11.1
flasgger==0.9.7.1

# Production server
gunicorn==21.2.0
eventlet==0.35.2
gevent==24.2.1

# PostgreSQL support (optional - for production deployments)
psycopg2-binary==2.9.9

# Browser automation (required for JS-heavy sites like Mercari, Poshmark, Facebook)
# After installing, run: playwright install chromium
playwright>=1.49.0

# Advanced anti-detection scraping
curl_cffi>=0.7.0  # TLS fingerprint impersonation (Chrome, Firefox, Safari)
fake-useragent>=1.4.0  # Real browser user agent database

# ======================
# SCRAPER ANTI-DETECTION SETUP
# ======================
# After pip install, run these commands:
#   1. playwright install chromium  - Install browser for JS-heavy sites
#   2. Set environment variables for proxies (optional but recommended):
#      - SCRAPER_PROXIES=http://user:pass@proxy1:8080,http://user:pass@proxy2:8080
#      - BRIGHTDATA_PROXY=http://user:pass@brd.superproxy.io:22225
#      - SMARTPROXY_PROXY=http://user:pass@gate.smartproxy.com:7000
#   3. Or create .proxies.txt with one proxy URL per line